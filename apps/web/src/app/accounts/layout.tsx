import "@heroui/styles/css";
import "@/components/layout/functional-shell.css";
import "@/components/market/market.css";

export default function AccountsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
