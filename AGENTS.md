@RTK.md

# 洲洲商行协作规范

## 开始工作

- `docs/` 只提交精简、当前有效的架构结论、接口契约和必要运行说明。进度、调研、开发计划、任务 prompt、评审过程及临时证据放在根 `tmp/`（Git 忽略）；不让公开文档依赖本地过程资料。更新结论时同步精简正式说明，不将完整过程重新搬回 `docs/`。

- 先读 `docs/architecture/technology-decisions.md` 和 `docs/README.md`；按任务需要读旧资料，不遍历或输出生产配置。
- 本地过程资料先查 `tmp/README.md`；当前进度只看 `tmp/docs/planning/rebuild-development-tracker.md` 与对应Master评审，执行brief不等于通过。已完成阶段放 `tmp/archive/`，不把历史prompt/快照当当前授权；正在执行的任务输入保持路径稳定，不遍历整个tmp或复制多份“最新状态”。
- 本地服务遵守 `docs/local-environments.md`：主环境固定用户站3100、管理站3101、API3102，由Master/指定维护者管理；先查主检出 `tmp/local-environments.md` 和监听归属，按需复用，不擅自启动三端、抢端口、停他人服务或重置数据。分支预览独立worktree并登记端口/数据资源。
- 本地业务验收先按 [测试快速登录](docs/local-test-auth.md) 使用 `npm run local:auth -- login --resource <资源集> --actor <身份>`，自动完成用户密码或管理员密码/TOTP并导入浏览器登录态，跳过人工认证操作。Owner 已授权此工作方式，无须每轮重复确认；认证专项才完整走登录/初始化页面。环境维护者首次用 `ensure --input` 登记现有 fixture；凭据集中保存在主检出 `apps/api/.secrets/local-auth/`，随环境复用，停服务不删除。禁止为登录问题重建/重置数据库、重复初始化 Boss、重新随机生成整套账号或认证加密 secret；查错按该文档执行。工具不改变实际角色、冻结和对象权限。
- PostgreSQL资源遵守 `docs/local-postgresql.md`；建库或测试前查主检出 `tmp/postgresql-resources.md`。同任务返修复用资源集，不逐轮新建随机库；完成整合后清理专用库与角色，保留资源必须登记用途与下一步。
- 仓库根目录是本文件所在目录。上级 `../analysis`、`../reports`、`../.archive` 是受控旧平台证据，不是新代码工作区。
- 默认中文沟通。区分已确认事实、代码推断、未知项和设计建议，结论附文件/方法或官方来源。
- 修改前检查 Git 状态，保留用户及其他 agent 的改动；不要未经任务授权提交、推送或重写 Git 历史。
- 保持仓库整洁：公共忽略规则集中在根 `.gitignore`，子目录仅在确有局部需求时增加规则，避免重复或过宽忽略掩盖源码、迁移和测试。环境模板仅含占位值；本地配置、构建产物及临时验证文件不混入源码。交接前检查全部未跟踪文件，只清理本人本任务创建且确认无用的残留，不清理归属不明文件；`.gitignore` 变更也须审阅并验证匹配范围。
- `.agents/`、`skills-lock.json` 与 `.claude/skills/` 属于本机技能/安装状态，不加入 Git；已跟踪项仅从索引移除，保留本地技能可用。

## Serena 项目范围

- 禁止在 monorepo 根目录或其父目录注册、创建配置或激活 Serena project。禁止 `activate_project(".")` 在根目录执行。
- 仅注册三个项目：`zzsh-admin` → `apps/admin`、`zzsh-api` → `apps/api`、`zzsh-web` → `apps/web`。每次使用 Serena 前读 initial_instructions，并按当前任务 activate 对应项目；切换后确认名称和根路径。
- 跨应用任务按步骤切换项目，再使用该项目内相对路径；不要并发切换同一个 Serena 会话，也不要沿用上一项目的符号结果。单项目引用结果不代表整个 monorepo 的引用已穷尽。
- 修改共享 packages、根配置或文档时使用普通文件工具和受控 rg 搜索，跨端改动分别检查调用方；不得为方便把 Serena 根扩大到整个仓库。
- `.serena` 项目配置、initial_prompt、缓存、日志和 memories 均为本机状态，由根 `.gitignore` 整体忽略；项目约束只以本文件和 `docs/serena.md` 为准，不提交工具自动生成或重写的配置。注册不等于完成 onboarding，不自动写入项目或全局 memories。
- 具体映射与激活方式见 `docs/serena.md`。

## 架构与业务

- 后续代码设计、开发、修复及评审任务默认指定 `ponytail:ponytail`（full）。先读相关调用链、搜索已有模块/工具/测试，按“现有实现→标准库/原生能力→已安装依赖→最小新增”选择方案；在共同入口修根因，避免重复造轮子和为未来空建抽象。新增模块或依赖须说明现有能力缺口与当前需求；交付简述复用了什么、为何必须新增，并做相称验证。不得为缩短代码省略权限校验、事务/幂等、错误处理、多端边界或必要测试；不借此扩大为未授权的全仓重构。任务 prompt 应明确这一要求，避免旧 worktree 漏读主检出规范。
- npm workspaces monorepo：`apps/*` 放可部署应用，`packages/*` 只放已有实际共享需求的代码。
- NestJS + TypeScript、模块化单体、托管 PostgreSQL 已确认。其他技术与版本以选型记录状态为准。
- 业务规则集中在 API；客户端不能决定最终价格、授权范围、支付成功或退款到账。
- 首期重建主流程是在线选账号和支付后通过网易云信由真人客服履约，新订单每单独立群。不能自行假设自动上号、强制下线或自动交付。
- 交易数据采用关系模型；JSONB 属性有类型、稳定标识和版本。金额不用浮点作为账务真值。
- 多态历史关联必须包含来源类型，保留旧单号、资金语义和历史快照。
- 所有业务功能兼顾 Web、iOS、Android 和微信小程序，遵循 `docs/architecture/multi-client-bff.md`。各端经服务端 BFF 适配；BFF 不直连交易库、不重复业务规则。
- NestJS 业务层不能依赖 Next.js、Cookie/DOM 或小程序运行时；核心业务提供稳定、语言无关的接口，Server Actions 不作为多端唯一契约。
- Web CSS/shadcn 组件不等于原生可复用 UI；旧客户端兼容、幂等重试、跨端身份与对象授权是业务接口设计的一部分。

## 安全与验证

- 默认本地开发和离线验证。未经具体任务授权，不接生产、不启动旧应用、不调用真实短信/实名/资金/IM/签约服务。
- 不输出或提交密码、密钥、Token、身份证、银行卡、数据库快照及原始配置。`.gitignore` 不是秘密扫描，也不会保护已跟踪文件。
- 所有写接口在服务端执行身份、操作权限及对象归属检查；隐藏按钮不是授权。
- 资金变动、回调和后台任务必须有事务、幂等和可追溯记录；过期 worker 不能写回新租约任务。
- 验证原型不直接当生产代码；已知边界见旧资料中的 NestJS 主 Agent 复核意见。
- 做与变更相称的检查。涉及金额、权限、并发、迁移须有真实断言及失败路径；mock 不替代数据库锁或真实渠道验收。
- 报告实际运行的检查、结果及 NOT_RUN，不把未运行脚本、编译或健康检查称作完整验收。
- 不删除或移动上级原始证据。新增共享模块、依赖、服务和任务组件须有当前需求，不为未来空建架构。
