@RTK.md

# 洲洲商行协作规范

## 开始工作

- 先读 `docs/architecture/technology-decisions.md` 和 `docs/README.md`；按任务需要读旧资料，不遍历或输出生产配置。
- 仓库根目录是本文件所在目录。上级 `../analysis`、`../reports`、`../.archive` 是受控旧平台证据，不是新代码工作区。
- 默认中文沟通。区分已确认事实、代码推断、未知项和设计建议，结论附文件/方法或官方来源。
- 修改前检查 Git 状态，保留用户及其他 agent 的改动；不要未经任务授权提交、推送或重写 Git 历史。

## Serena 项目范围

- 禁止在 monorepo 根目录或其父目录注册、创建配置或激活 Serena project。禁止 `activate_project(".")` 在根目录执行。
- 仅注册三个项目：`zzsh-admin` → `apps/admin`、`zzsh-api` → `apps/api`、`zzsh-web` → `apps/web`。每次使用 Serena 前读 initial_instructions，并按当前任务 activate 对应项目；切换后确认名称和根路径。
- 跨应用任务按步骤切换项目，再使用该项目内相对路径；不要并发切换同一个 Serena 会话，也不要沿用上一项目的符号结果。单项目引用结果不代表整个 monorepo 的引用已穷尽。
- 修改共享 packages、根配置或文档时使用普通文件工具和受控 rg 搜索，跨端改动分别检查调用方；不得为方便把 Serena 根扩大到整个仓库。
- 项目配置与 initial_prompt 随 Git 管理；缓存、日志、memories 保留本地。注册不等于完成 onboarding，不自动写入项目或全局 memories。
- 具体映射与激活方式见 `docs/serena.md`。

## 架构与业务

- npm workspaces monorepo：`apps/*` 放可部署应用，`packages/*` 只放已有实际共享需求的代码。
- NestJS + TypeScript、模块化单体、托管 PostgreSQL 已确认。其他技术与版本以选型记录状态为准。
- 业务规则集中在 API；客户端不能决定最终价格、授权范围、支付成功或退款到账。
- 现有主流程是在线选账号和支付后由企业微信真人客服履约。不能自行假设自动上号、强制下线或自动交付。
- 交易数据采用关系模型；JSONB 属性有类型、稳定标识和版本。金额不用浮点作为账务真值。
- 多态历史关联必须包含来源类型，保留旧单号、资金语义和历史快照。
- 所有业务功能兼顾 Web、iOS、Android 和微信小程序，遵循 `docs/architecture/multi-client-bff.md`。各端经服务端 BFF 适配；BFF 不直连交易库、不重复业务规则。
- NestJS 业务层不能依赖 Next.js、Cookie/DOM 或小程序运行时；核心业务提供稳定、语言无关的接口，Server Actions 不作为多端唯一契约。
- Web CSS/shadcn 组件不等于原生可复用 UI；旧客户端兼容、幂等重试、跨端身份与对象授权是业务接口设计的一部分。

## 安全与验证

- 默认本地开发和离线验证。未经具体任务授权，不接生产、不启动旧应用、不调用真实短信/实名/资金/IM/签约服务。
- 不输出或提交密码、密钥、Token、身份证、银行卡、数据库快照及原始配置。`.gitignore` 不是秘密扫描，也不会保护已跟踪文件。
- 所有写接口在服务端执行身份、操作权限及对象归属检查；隐藏按钮不是授权。
- 资金变动、回调和后台任务必须有事务、幂等和可追溯记录；过期 worker 不能写回新租约任务。
- 验证原型不直接当生产代码；已知边界见旧资料中的 NestJS 主 Agent 复核意见。
- 做与变更相称的检查。涉及金额、权限、并发、迁移须有真实断言及失败路径；mock 不替代数据库锁或真实渠道验收。
- 报告实际运行的检查、结果及 NOT_RUN，不把未运行脚本、编译或健康检查称作完整验收。
- 不删除或移动上级原始证据。新增共享模块、依赖、服务和任务组件须有当前需求，不为未来空建架构。
